/**
 * Empty stub. The real @deepseek-ai/dsh-type-meta is registry-restricted; it is
 * a types-only peer of @deepseek-ai/dsh-session (no runtime import), and every
 * package here compiles with skipLibCheck, so an empty declaration suffices.
 */
export {}
